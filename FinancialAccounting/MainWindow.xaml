<Window x:Class="FinancialAccounting.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Финансовый учёт" Height="700" Width="1000"
        Background="#FFF7F9FC" Foreground="#1F2937"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>

        <SolidColorBrush x:Key="PrimaryBrush" Color="#1565C0"/>
        <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#0D47A1"/>
        <SolidColorBrush x:Key="PrimaryPressedBrush" Color="#0A3276"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#D32F2F"/>
        <SolidColorBrush x:Key="DangerHoverBrush" Color="#B71C1C"/>
        <SolidColorBrush x:Key="DangerPressedBrush" Color="#8E1616"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="CardBrush" Color="#FFFDFDFD"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#FFD6DEE8"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#FF6B7280"/>

        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="6,0,6,0"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="MinWidth" Value="130"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                SnapsToDevicePixels="True">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10"
                                                  ShadowDepth="2"
                                                  Opacity="0.12"
                                                  Color="#000000"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource PrimaryHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource PrimaryPressedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="MinWidth" Value="95"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="DangerBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                SnapsToDevicePixels="True">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10"
                                                  ShadowDepth="2"
                                                  Opacity="0.10"
                                                  Color="#000000"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="DangerBorder" Property="Background" Value="{StaticResource DangerHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="DangerBorder" Property="Background" Value="{StaticResource DangerPressedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="DangerBorder" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AddButtonStyle" TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
            <Setter Property="MinWidth" Value="145"/>
            <Setter Property="Padding" Value="16,10"/>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="16"
                                      ShadowDepth="3"
                                      Opacity="0.08"
                                      Color="#000000"/>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Border Grid.Row="0"
                Background="{StaticResource SurfaceBrush}"
                CornerRadius="14"
                Padding="14"
                Margin="0,0,0,14"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1">
            <DockPanel LastChildFill="False">
                <TextBlock Text="Финансовый учёт"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="#111827"
                           VerticalAlignment="Center"
                           Margin="6,0"
                           DockPanel.Dock="Left"/>

                <Button Content="Выход"
                        Style="{StaticResource DangerButtonStyle}"
                        Margin="0,0,6,0"
                        Click="Logout_Click"
                        DockPanel.Dock="Right"/>

                <TextBlock x:Name="UsernameTextBlock"
                           Text="Пользователь:"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource MutedTextBrush}"
                           Margin="16,0,0,0"/>
            </DockPanel>
        </Border>

        <!-- Панель навигации -->
        <Border Grid.Row="1"
                Background="{StaticResource SurfaceBrush}"
                CornerRadius="14"
                Padding="10"
                Margin="0,0,0,12"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Style="{StaticResource NavButtonStyle}" Content="Загрузка данных" Click="Button_Click"/>
                <Button Style="{StaticResource NavButtonStyle}" Content="Отчёты" Click="Button_Click_1"/>
                <Button Style="{StaticResource NavButtonStyle}" Content="Аналитика" Click="Button_Click_2"/>
                <Button Style="{StaticResource NavButtonStyle}" Content="Настройки" Click="Button_Click_3"/>
            </StackPanel>
        </Border>

        <!-- Счета -->
        <Border Grid.Row="2"
                Background="{StaticResource SurfaceBrush}"
                CornerRadius="14"
                Padding="14"
                Margin="0,0,0,12"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="❔"
                           FontSize="18"
                           ToolTip="Выберите счёт для анализа данных."
                           VerticalAlignment="Center"
                           Margin="0,0,8,0"
                           Foreground="{StaticResource MutedTextBrush}"/>

                <ComboBox x:Name="AccountComboBox"
                          Width="220"
                          Height="38"
                          Margin="0,0,12,0"
                          VerticalAlignment="Center"
                          Background="White"
                          Foreground="#111827"
                          FontWeight="SemiBold"
                          BorderBrush="{StaticResource PrimaryBrush}"
                          SelectionChanged="AccountComboBox_SelectionChanged"/>

                <Button Content="Добавить счёт"
                        Style="{StaticResource AddButtonStyle}"
                        Click="AddAccount_Click"/>
            </StackPanel>
        </Border>

        <!-- Графики -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Расходы -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="График расходов"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,6"
                               FontWeight="Bold"
                               FontSize="16"
                               Foreground="#111827"/>

                    <lvc:PieChart Name="ExpenseChart"
                                  MinHeight="300"
                                  LegendLocation="Bottom"
                                  Foreground="#1F2937">
                        <lvc:PieChart.Resources>
                            <Style TargetType="lvc:PieSeries">
                                <Setter Property="Foreground" Value="#1F2937"/>
                                <Setter Property="FontSize" Value="14"/>
                            </Style>
                        </lvc:PieChart.Resources>

                        <lvc:PieChart.DataTooltip>
                            <lvc:DefaultTooltip SelectionMode="OnlySender" />
                        </lvc:PieChart.DataTooltip>
                    </lvc:PieChart>
                </StackPanel>
            </Border>

            <!-- Доходы -->
            <Border Style="{StaticResource CardStyle}" Grid.Column="1">
                <StackPanel>
                    <TextBlock Text="График доходов"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,6"
                               FontWeight="Bold"
                               FontSize="16"
                               Foreground="#111827"/>

                    <lvc:PieChart Name="IncomeChart"
                                  MinHeight="300"
                                  LegendLocation="Bottom"
                                  Foreground="#1F2937">
                        <lvc:PieChart.Resources>
                            <Style TargetType="lvc:PieSeries">
                                <Setter Property="Foreground" Value="#1F2937"/>
                                <Setter Property="FontSize" Value="14"/>
                            </Style>
                        </lvc:PieChart.Resources>

                        <lvc:PieChart.DataTooltip>
                            <lvc:DefaultTooltip SelectionMode="OnlySender" />
                        </lvc:PieChart.DataTooltip>
                    </lvc:PieChart>
                </StackPanel>
            </Border>
        </Grid>

    </Grid>
</Window>